@page
@model Mehno_Gavril_Volunteers.Pages.Volunteers.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Volunteer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Volunteer.ID" />
            <div class="form-group">
                <label asp-for="Volunteer.Name" class="control-label"></label>
                <input asp-for="Volunteer.Name" class="form-control" />
                <span asp-validation-for="Volunteer.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Volunteer.Department" class="control-label"></label>
                <input asp-for="Volunteer.Department" class="form-control" />
                <span asp-validation-for="Volunteer.Department" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Volunteer.Occupation" class="control-label"></label>
                <input asp-for="Volunteer.Occupation" class="form-control" />
                <span asp-validation-for="Volunteer.Occupation" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Volunteer.EntryDate" class="control-label"></label>
                <input asp-for="Volunteer.EntryDate" class="form-control" />
                <span asp-validation-for="Volunteer.EntryDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Volunteer.PublisherID" class="control-label">Publisher Name</label>
                <select asp-for="Volunteer.PublisherID" class="form-control" asp-items="ViewBag.PublisherID">
                </select>
                <span asp-validation-for="Volunteer.PublisherID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCategories"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
